/* 
 * Operating Systems - Homework One
 * Victor Frenkel
 */

#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#include "sllist.h"
#include "shell.h"

int process_input(char *input) {
  // parse and keep a list of commands with pointers to their argument
  // lists (which can be empty).
  SLList commands;
  

  //parse the input
  
  return 0;
}
